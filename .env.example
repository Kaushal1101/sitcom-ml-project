# Copy to `.env` and fill in values. Never commit `.env`.

# --- Database (SQLite; see backend/db/session.py) ---
# DATABASE_URL=sqlite:///data/app.sqlite3
SQLITE_PATH=data/app.sqlite3

# --- Optional: HTTP scraping (User-Agent rotation reads from env if set) ---
# HTTP_USER_AGENT=
