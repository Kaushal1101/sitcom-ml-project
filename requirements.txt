# --- Scraping (Dunderpedia / Fandom MediaWiki API) ---
httpx>=0.27.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
python-dotenv>=1.0.0

# --- Pipeline (LangGraph: scrape → ingest) ---
langgraph>=0.2.0

# --- Optional future deps (e.g. Reddit client) — add when needed ---
# praw>=7.7.0
